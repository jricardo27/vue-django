// Small JS elements to facilitate UI usage.

export default {
    init: () => {
        // Setup JS page elements.
        $(document).ready(() => {
            // Initialise elements here.
        });
    },
};
