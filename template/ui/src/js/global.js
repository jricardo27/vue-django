/**
 * Small Javascript elements to facilitate UI usage.
 */

export default {
    init: () => {
        /*
        $( document ).ready(() => {
            // Initialise Select2.
            $(".select2").select2({width: '100%'});
        });
        */
    },
};
